doctrine:
    dbal:
        url: '%env(resolve:DATABASE_URL)%'

    orm:
        auto_mapping: false

        mappings:
            User:
                type: attribute
                dir: '%kernel.project_dir%/src/User/Infrastructure/Doctrine/Entity'
                prefix: 'App\User\Infrastructure\Doctrine\Entity'
                alias: User